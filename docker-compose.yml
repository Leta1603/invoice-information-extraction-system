version: "3.9"

services:
  backend:
    build: ./backend
    container_name: invoice_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./models:/app/models
    env_file:
      - .env